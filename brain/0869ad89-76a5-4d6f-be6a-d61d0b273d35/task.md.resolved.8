# Handle Duplicate Barcodes & Master Data Updates

- [x] Modify [models.py](file:///c:/Users/dkstk/.gemini/antigravity/models.py) to remove `unique=True` from `ProductMaster.barcode`.
- [x] Update `/api/products/{barcode}` in [main.py](file:///c:/Users/dkstk/.gemini/antigravity/main.py) to return a list of matched products instead of just the first one.
- [x] Update `/api/upload/master` in [main.py](file:///c:/Users/dkstk/.gemini/antigravity/main.py) to update existing records and append new ones without deleting the entire table (partially already there, but needs to handle multiple identical barcodes correctly if the upload logic currently assumes 1:1).
- [x] Modify [static/js/app.js](file:///c:/Users/dkstk/.gemini/antigravity/static/js/app.js) to handle a list of returned products:
  - If 1 product, proceed as normal.
  - If > 1 product, display a modal or a list of radio buttons to let the worker select the correct item before proceeding to the quantity input UI.
- [x] Update [templates/index.html](file:///c:/Users/dkstk/.gemini/antigravity/templates/index.html) to add a product selection modal/UI component for the worker view.

# Debug Return Plan & Dynamic Store Select
- [x] Add `/api/stores` endpoint to [main.py](file:///c:/Users/dkstk/.gemini/antigravity/main.py) that queries [ReturnPlan](file:///c:/Users/dkstk/.gemini/antigravity/models.py#13-20) and returns a list of unique store names.
- [x] Update frontend [index.html](file:///c:/Users/dkstk/.gemini/antigravity/templates/index.html) to populate the `select-store` dropdown dynamically upon page load using the `/api/stores` endpoint (keeping '전체' for Master).
- [x] Debug the [upload_plan](file:///c:/Users/dkstk/.gemini/antigravity/main.py#283-342) API in [main.py](file:///c:/Users/dkstk/.gemini/antigravity/main.py). The user reports it is empty despite a successful upload message. It might be due to column header names in their Excel file not matching `'STORE_NAME'`, `'ITEM_CODE'`, or `'ORDER_QUANTITY'`. We need to handle fallback column names like '점포명', '상품코드', '수량' or '예정수량'.

# Location Assignment & Defaults
- [ ] Add `fixed_cell` Column to [ProductMaster](file:///c:/Users/dkstk/.gemini/antigravity/models.py#5-12) in [models.py](file:///c:/Users/dkstk/.gemini/antigravity/models.py).
- [ ] Make `qty-normal` input default to 1, and reset to 1 in [app.js](file:///c:/Users/dkstk/.gemini/antigravity/static/js/app.js).
- [ ] Create `GET /api/aggregate-plans` and `PUT /api/products/{code}/cell` in [main.py](file:///c:/Users/dkstk/.gemini/antigravity/main.py).
- [ ] Add "로케이션 배정" (Location Assignment) tab in [index.html](file:///c:/Users/dkstk/.gemini/antigravity/templates/index.html).
- [ ] Implement `loadAggregatedPlans()` to fetch and show sorted data with cell input fields in [app.js](file:///c:/Users/dkstk/.gemini/antigravity/static/js/app.js).
